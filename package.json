{
  "name": "sistema-solar",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": "18.x" },
  "dependencies": {
    "@react-three/drei": "9.106.2",
    "@react-three/fiber": "8.15.16",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-scripts": "5.0.1",
    "three": "0.160.0"
  },
  "overrides": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": { "extends": ["react-app", "react-app/jest"] },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
git init
git remote set-url origin https://github.com/Tommyy-spec/sistema-solar.git
git add -A
git commit -m "Versión final lista para deploy"
git branch -M main
git push -u origin main --force
